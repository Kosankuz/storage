$(document).ready(function(){
  var privateSale = 'Private-Sale',
      preSale = 'Pre-Sale',
      publicSale = 'Public-Sale',
      privateStartDate ,
      privateEndDate,
      preStartDate,
      preEndDate,
      publicStartDate,
      publicEndDate;

  $("#PrivateSale").text(privateSale);
  $("#PreSale").text(preSale);
  $("#PublicSale").text(publicSale);

//  var date = new Date();
//alert(date.toDateString());
});
